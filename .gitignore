# Generated by XcodeGen — project.yml is the source of truth.
Gancho.xcodeproj/

# Xcode / SwiftPM
.build/
DerivedData/
build/
dist/
xcuserdata/
*.xcresult
*.profraw

# Local UI and browser validation evidence.
output/

# Vendored frameworks (fetched by scripts, never committed)
Vendor/

# macOS noise
.DS_Store

# Local planning, audits, and agent workflow — maintainer-only, never committed.
.planning/
.audit/
.claude/
.agents/

# Pulled design system from Claude Design (reference only, never committed).
docs/design/
